{
  "name": "unified-angular-node",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:bun": "concurrently --kill-others \"npm run dev:bun -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w shared && npm run build -w frontend && npm run build -w backend",
    "build:bun": "npm run build -w shared && npm run build -w frontend",
    "start": "npm run start -w backend",
    "start:bun": "npm run start:bun -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
